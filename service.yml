

apiVersion: v1
kind: Service
metadata:
  name:  webapp_service
  labels:
    app: webapp
    version: "1.0"
spec:
  selector:
    app:  webapp  
  type: NodePort
  ports:
  - name: TCP
    port: 80
    targetPort:  8080